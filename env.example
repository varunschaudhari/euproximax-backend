# ============================================
# EuProximaX Backend - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# ============================================
# Domain Configuration
# ============================================
# Production Domains
# WEBSITE_URL=https://euproximax.com
# ADMIN_URL=https://admin.euproximax.com
# API_URL=https://api.euproximax.com

# Development URLs (for local development)
WEBSITE_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
API_URL=http://localhost:3000

# ============================================
# Server Configuration
# ============================================
PORT=3000
NODE_ENV=development
# Production: NODE_ENV=production
API_VERSION=v1

# ============================================
# Database Configuration
# ============================================
# MongoDB Connection String
# Format: mongodb://[username:password@]host[:port][/database][?options]
MONGODB_URI=mongodb://localhost:27017/euproximax
# Alternative: DATABASE_URL=mongodb://localhost:27017/euproximax

# ============================================
# JWT Authentication Configuration
# ============================================
# IMPORTANT: Generate strong, random secrets for production
# Use: openssl rand -base64 32 (for each secret)

# Main JWT secret for token signing
JWT_SECRET=default-secret-key-change-in-production

# Payload encryption secret
JWT_PL_SECRET=default-payload-secret-key-change-in-production

# JWT salt for additional security
JWT_SALT=default-salt-change-in-production

# JWT token expiration time (e.g., 1d, 7d, 24h)
JWT_EXPIRE=1d

# ============================================
# Email Configuration (SMTP)
# ============================================
# Hostinger SMTP Settings
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
# For SSL (port 465), set SMTP_SECURE=true

# SMTP Authentication
SMTP_USER=contact@euproximax.com
SMTP_PASS=your-spSFLB/R0

# Email From Address
MAIL_FROM=contact@euproximax.com

# Logo URL for email templates (full URL or relative path)
LOGO_URL=PNG.png
# Production: LOGO_URL=https://euproximax.com/PNG.png

# ============================================
# OpenAI Configuration (for Chatbot)
# ============================================
# OpenAI API Key for chatbot functionality
# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-proj-your-openai-api-key-here-change-this

# OpenAI Model to use for chatbot responses
# Default: gpt-4o-mini (recommended for cost-effectiveness)
# Other options: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# ============================================
# Admin Portal Configuration
# ============================================
# Admin portal URL (for project approval links, etc.)
# Development: ADMIN_PORTAL_URL=http://localhost:5174
ADMIN_PORTAL_URL=https://admin.euproximax.com
# Production: ADMIN_PORTAL_URL=https://admin.euproximax.com

# ============================================
# Google Meet Configuration
# ============================================
# Base URL for Google Meet links
GOOGLE_MEET_BASE_URL=https://meet.google.com

# Meeting code format (default: 3-4-3, e.g., abc-defg-hij)
# You can customize the length of each part
GOOGLE_MEET_PART1_LENGTH=3
GOOGLE_MEET_PART2_LENGTH=4
GOOGLE_MEET_PART3_LENGTH=3

# Character set for generating meeting codes (default: lowercase letters)
# GOOGLE_MEET_CHARSET=abcdefghijklmnopqrstuvwxyz

# Note: The generated links follow Google Meet's format but won't be actual working Meet links
# until created through Google Calendar API or manually in Google Calendar.
# For production, integrate with Google Calendar API to create real Meet links.

# ============================================
# Logging Configuration
# ============================================
# Log levels: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# CORS Configuration
# ============================================
# CORS origins are automatically configured from WEBSITE_URL and ADMIN_URL above
# Additional origins can be added in server.js if needed

# ============================================
# Notes
# ============================================
# 1. Never commit .env file to version control
# 2. Use different secrets for development and production
# 3. Keep JWT secrets secure and rotate them periodically
# 4. Update SMTP credentials with actual Hostinger email credentials
# 5. Ensure MongoDB is accessible from your server
# 6. For production, use strong, randomly generated secrets
# 7. OpenAI API key is required for chatbot functionality
# 8. ADMIN_PORTAL_URL is used for generating approval links in emails
# 9. Google Meet links are generated automatically but require Google Calendar API integration for actual working links

